version: '3.8'

services:
  react-app:
    build: .
    container_name: meu-react-container
    
    # MUDANÇA: Mude de 3000 para 5173
    ports:
      - "5173:5173"
    
    volumes:
      - ./src:/app/src
      - /app/node_modules
    
    environment:
      - CHOKIDAR_USEPOLLING=true